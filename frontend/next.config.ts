import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  output: 'standalone',
  outputFileTracingRoot: '/Users/illanmastey/epitech/SimulatedProject/EpiTrello/frontend',
};

export default nextConfig;
